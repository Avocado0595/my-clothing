import React from 'react';
import PropTypes from 'prop-types';

NumberUpdown.propTypes = {
    
};

function NumberUpdown(props) {
    return (
        <div>
            
        </div>
    );
}

export default NumberUpdown;